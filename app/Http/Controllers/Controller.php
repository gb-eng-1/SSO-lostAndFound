<?php

namespace App\Http\Controllers;

/**
 * Base controller.
 * All other controllers extend this class.
 * In a full Laravel install this would extend Illuminate\Routing\Controller
 * and use the AuthorizesRequests and ValidatesRequests traits.
 */
abstract class Controller
{
    // Base controller — add shared middleware/helpers here if needed.
}
